name: deloy

on:
  push:
    branches: [master]
    
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:       
      - name: checkout code
        uses: actions/checkout@v2
       
      - name: run build
        run: docker-compose up -d
      
